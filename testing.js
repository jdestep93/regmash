const { longestCommonSubstring } = require('string-algorithms');
const { regex } = require('./src') ;

const strs = [
    // 'baconing',
    // 'reckoning',
    'argon',
    'carbon',

]

console.log(regex(strs));
